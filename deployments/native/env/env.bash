# shellcheck disable=SC2139

export VERAISON_ROOT=${VERAISON_ROOT}

alias veraison="${VERAISON_ROOT}/bin/veraison"
alias cocli="${VERAISON_ROOT}/bin/veraison cocli"
alias evcli="${VERAISON_ROOT}/bin/veraison evcli"
alias pocli="${VERAISON_ROOT}/bin/veraison pocli"

[Unit]
Description=Veraison CoSERV API server
After=veraison-trusted.service

[Service]
Type=exec
ExecStart=${VERAISON_BIN_DIR}/coserv-service --config ${VERAISON_CONFIG_DIR}/services/config.yaml
Restart=always
User=${VERAISON_USER}

[Install]
WantedBy=default.target
Requires=veraison-trusted.service

# vim: set ft=systemd:
